

export default function() {
  // Iterate through ../../_posts
  // Take naming convention from Jekyll
  // Write package.json script that scaffolds a post with correct current format, and other default info (look at jekyll)
}
