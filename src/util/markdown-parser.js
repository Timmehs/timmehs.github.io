
// TODO: A class (or maybe just a helper fn) to convert MD files to JSX
class MarkdownParser {
  constructor(file) {
    this.file = file
  }

  toJSX() {
    return (
      // MD to JSX
    )
  }
}
